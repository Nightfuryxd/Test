apiVersion: v1
kind: Pod
metadata:
  name: demo-kotlinapp
  labels:
    role: webapp
spec:
  containers:
    - name: web
      image: 9425424282/demo-kotlintest
      ports:
        - containerPort: 80
          name: web
          protocol: TCP